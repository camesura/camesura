# mobile

Android / iOS向けの「かめすら」Flutterアプリです。

## 起動

リポジトリのルートでmiseによるセットアップを済ませてから実行します。

```sh
mise install
mise run setup
cd mobile
flutter run
```

現在はプロダクトの役割を伝えるトップ画面と補正準備画面まで実装しています。カメラ、姿勢推定、Bridge通信は次のマイルストーンで追加します。
