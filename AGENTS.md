# AGENTS.md

- エミュレータを起動して動作確認が必要になる作業（UI の見た目修正、実機でしか検証できない複雑なもの、テスト）では、**エミュレータを起動しない**。
- 起動・ビルド・実機確認はユーザー自身が行うものとし、エージェントは可能な範囲の編集と変更内容の説明までで**すぐに終わらせる**こと。

## リリースノート

- リリースノートに載る `feat:` / `fix:` のコミット件名は、必ず日本語で書く。
- 実装した機能や内部構造ではなく、リリース後にユーザーができるようになったこと・体験できることを書く。
- `feat:` はユーザーに新しい価値を提供する変更、`fix:` はユーザーが遭遇する不具合の修正に使う。内部のリファクタリング、テスト、CI、ドキュメントなど、ユーザー向けリリースノートに載せない変更には `refactor:`、`test:`、`ci:`、`docs:`、`chore:` を使う。
- 実装名だけの件名（例: `feat: Aポーズ判定クラスを追加`）ではなく、ユーザー視点の結果を書く（例: `feat: Aポーズを取るとトラッキングを開始できる`）。修正も可能な限り同様に書く（例: `fix: Bridge接続中もポーズ検出が滑らかに動くように修正`）。
- squash mergeするPRでは、PRタイトルが上記のコミット件名になるようにする。
